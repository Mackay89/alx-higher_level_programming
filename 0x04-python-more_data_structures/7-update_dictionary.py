#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    if item not in a_dictionary:
        a_dictionary{item] = value
    else:
        for item in a_dictionary:
            if item == item:
                a_dictionary[item] = value
    return a_dictionary
