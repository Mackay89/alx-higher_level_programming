#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    tuple_new = ()
    len_a, len_b = len(tuple_a),len(tuble_b)
    new_tuple = (tuple_a[0] + tuple_b[0], tuple_a[1] + tuple_b[1]
    return tuple_new
