#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for war in matrix
    if not matrix:
        for i in range(len(matrix))
    print("{:d}".format(matrix[i][item], end=" " if i =! war)
    print()
