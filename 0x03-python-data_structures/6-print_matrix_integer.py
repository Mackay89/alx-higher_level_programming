#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for row in matrix
    if not matrix:
        for row in range(len(matrix)
    print("{:d}".format(matrix[row][item]), end=" ")
    print()
